block content
    h1 Lista de Autores
    table
      thead
        tr
          th ID_Autor
          th Nombre
          th Pais
          th Acciones
      tbody
        if autores && autores.length
          each autor in autores
            tr
              td= autor.id_autor
              td= autor.nombre
              td= autor.pais
              td
                a(href=`autor/autor-edit/${autor.id_autor}`) Editar
                span | 
                a(href=`autor/autor-delete/${autor.id_autor}`) Eliminar
        else
          tr
            td(colspan="4") No hay autores disponibles.
    p
      a(href='autor/autor-add') Agregar Nuevo Autor

    h2 Maestro detalle: Libros escritos por un autor
    
    label(for="libros") Seleccione un libro:
    select(name="libros", id="libros", onchange="location = 'libro/libro/' + this.value;")
      option(value="") -- Seleccione un autor --
      if autores && autores.length
        each autor in autores
          option(value=autor.id_autor)= autor.nombre
